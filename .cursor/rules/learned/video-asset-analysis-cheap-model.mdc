---
description: 视频素材画面粗分析用便宜模型
alwaysApply: true
---

# 视频素材分析成本

- 对备选 B-roll / 填空素材做抽帧、时间段画面描述时，**默认用便宜模型**（Task：`composer-2.5-fast` 或当时等价低价档）。
- 只记时间段 + 简述 + 标签，相近画面合并；分析结果落盘复用。
- 成片语义匹配、字幕、最终验收仍用主会话模型。
