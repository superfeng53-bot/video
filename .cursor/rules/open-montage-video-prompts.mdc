---
description: AI 生视频提示词一律走 OpenMontage skill 链（生成 + 验收）
globs: "**/*生视频*.md,**/*完整方案.md,**/*故事板*.md,**/*提示词*.md,**/*口播*.md,**/scripts/*.md"
alwaysApply: false
---

# OpenMontage · AI 生视频提示词

在本仓库 **写、改、审** AI 生视频提示词时，**禁止**凭记忆或旧 Koda / 全局 `seedance-2.0-temp` 写法。**必须先读 skill，再动笔，交付前过验收清单。**

## 生成前必读（按顺序）

1. `AI工具/OpenMontage/skills/creative/video-gen-prompting.md` — 5 维骨架（Subject / Motion / Scene / Spatial / Camera）与词汇表
2. 按目标模型读 `AI工具/OpenMontage/skills/creative/prompting/<model>-prompting.md`
   - 默认 **Seedance 2.0** → `seedance-prompting.md`
3. Layer 3 → `.cursor/skills/seedance-2-0/SKILL.md`（或实际选用模型的 Layer 3 skill）

## 写提示词

- 默认模型：**Seedance 2.0**（`standard`；多镜头 / 运镜重时用完整结构，预览才用 `fast`）
- 多镜头：分 beat **直接重复**身份描述（发型、服装、标志物），禁止只用「同一人 / 他」；**`@` 引用仅开头绑定一次**（见下节）
- 运镜：dolly ≠ zoom，pan ≠ truck；写 `static` 则零运镜零变焦
- 情绪词 → 可见视觉原因（见 `video-gen-prompting.md` What to Avoid）
- 竖屏 9:16、口播对白、reference 图：按 `seedance-prompting.md` + `seedance-2-0` 对应段落

## 口播台词 · 语速与拆轨

**语速：约 6 字/秒，标点计入字数。**

| 规则 | 说明 |
| --- | --- |
| 换算 | `口播秒数 ≈ 字数 ÷ 6`（规划时四舍五入到整数秒） |
| 锚点 | **62 字 → 10 秒** · **93 字 → 15 秒** |
| 单轨上限 | **最长 15 秒**；口播段 **≤ 93 字（含标点）** |
| 超限 | 字数 **> 93** 或推算口播 **> 15 秒** → **必须拆轨**；每轨独立 ≤93 字且 ≤15 秒，并标注轨号 / 起止秒 |

写 `-生视频提示词.md` 或 `-完整方案.md` 时，每个口播段须附：**字数 · 推算秒数 · 是否合规**；拆轨须写清各轨台词与对应 Seedance 时长参数。

## 音频参考 · 按轨挂载（强哥音色）

**@音频1 / {{Mixed 1}} 等强哥音色参考，只在本轨实际生成强哥口播时才上传并写进可复制块。**

| 规则 | 说明 |
| --- | --- |
| **按轨判断** | 逐轨看「本轨谁对口型、谁生成配音」——不是全片统一挂音频 |
| **有强哥口播** | 本轨含强哥 `says` / 对口型段 → 上传 @音频1，写 `@音频1 = 仅强哥四川话音色参考`（锁音色+口音，非全片配音轨） |
| **无强哥口播** | 本轨强哥仅画面（嘴闭、验厨、过渡、CTA 在别轨）→ **`[H]` 上传表标注不上传 @音频1**；交付文本 **省略 @音频 行**，用正向口播描述（如 `阿姨口播：四川话女声`） |
| **音色不扩散** | `[H]` 验收：强哥 @音频 不挂无强哥口播轨；交付文本 **不写**「禁止模仿强哥」等纠偏句 |

拆轨时：上传素材表、各轨 Seedance 可复制块里的 `@音频` 行 **按轨分别列**；全局素材表若混写，须标注「仅轨 N」。**无强哥口播的轨：不上传 @音频，且交付文本里也不写「不上传/不引用」等 meta 句**——用 `[H]` 上传表表达即可。

## 强哥口播 · 四川话铁律

本仓库 **强哥凡有对口型口播**，须 **四川话**（默认 **成都温江本地口音**），不得出普通话/北方口音念稿。

| 层 | 写法 |
| --- | --- |
| **`[H]` 本片规定** | 强哥行写 **口播四川话** · 可加 **强哥语言** 行：`@音频1` 锁 **四川话音色+口音** · **禁止普通话念稿** |
| **上传表 @音频1** | `强哥四川话音色参考` · 标注 **锁音色+成都温江口音** |
| **交付块绑定** | `@音频1 = 强哥四川话音色参考；强哥口播对口型，成都温江四川话，气势实在洪亮，禁止普通话念稿。` |
| **交付块口播行** | 另起一行 **`强哥口播：四川话男声，成都温江本地口音`**（有强哥口播的轨/单轨必写） |
| **分 beat** | 强哥 `说` 段 performance 后标 **`——四川话`** 或 **`——四川话洪亮…`** |
| **验收** | 有强哥口播 → 已挂 @音频1 · 交付块有 **强哥口播：四川话** · 分 beat 已标四川话 |
| **翻车** | 强哥变普通话 → 补绑定+口播行+分 beat 四川话标记 · 重出 |

> 其他说话人（阿姨/男业主/画外音）仍写 **独立四川话声线**，**禁止模仿强哥音色**——与强哥 **必须四川话** 不矛盾。

## 双轨续拍 · 业主手持 + 末帧参考

**talk-multi 双轨若均为业主阿姨手持自拍**，轨 2 须与轨 1 **机位对齐**，用 **轨 1 生成末帧** 作接续参考——**不是**再挂一张阿姨五官定妆图。

| 规则 | 说明 |
| --- | --- |
| **双轨 POV** | 轨 1、轨 2 交付文本均写 **业主阿姨手持手机自拍**；轨 2 不写切到强哥/third-person 机位 |
| **轨 1 末帧** | 轨 1 生成后截取 **最后一帧** → 上传轨 2 的 `@图片N`（如 `@图片5`） |
| **末帧绑什么** | 机位角度、窗光、厨房背景、阿姨侧后站位、强哥起始位置——**接续对齐** |
| **轨 2 不上阿姨五官图** | 轨 2 **省略**轨 1 用的阿姨定妆 `@图片`（如 @图片4）；阿姨形象 **以首帧参考图为准** |
| **强哥脸** | 轨 2 仍挂强哥 `@图片2` 锁脸；动作写 **深景→中近景对镜**，不写占自拍前景 |

`[H]` 上传表须 **按轨分列** `@图片4 仅轨1` / `@图片5 仅轨2（末帧）`；轨 2 可复制块 **`@图片5` 置顶**（续拍锚点优先于环境图）。

**轨 2 可复制块须自洽**：生成时 **单独粘贴**，模型 **看不到轨 1 或其它 `[H]`**。续拍图在 `[H]` 写「轨 1 末帧截图 → 上传」；交付文本 **`@图片5 = 作为首帧` + 以参考图为准**，**不展开阿姨外貌/姿态**（轨 1 成片可能与文案有偏差，以截图为准）。强哥写 **自深景向镜头走来、至中近景对镜口播**。**不写**「轨1」「上一段」「末帧来源」「占自拍前景」。

## 交付块逻辑自检 · 空间 / 机位 / 续拍（治标治本）

> 写稿人易把 **整份文档上下文** 写进某一轨；模型生成时 **每轨单独粘贴**，且 **上一段成片常与文案有偏差**。以下四条须 **逐轨** 自检，**举一反三**：凡 **续拍帧 / 接力生成 / 手持 POV / 双角色同框** 均适用，不限 talk-multi。

### 1 · 单独粘贴测试（文档上下文隔离）

| 检查 | 通过标准 |
| --- | --- |
| **隔离** | 只读该轨可复制块 + 该轨上传素材，能否独立生成？ |
| **禁跨轨 meta** | 交付块 **无**「轨1/轨2/上一段/下一段/末帧截图来源/与轨N对齐」 |
| **禁策划数字** | 交付块 **无** 语速换算、字数÷6、策划表秒数（仅 `[H]`） |
| **续拍绑法** | `@图片N = 作为首帧` + **以参考图为准**；截图流程 **只在 `[H]`** |

### 2 · 续拍以图为准（少复述、防前后打架）

| 检查 | 通过标准 |
| --- | --- |
| **信任截图** | 续拍轨对 **已由首帧/尾帧/接续图锁定** 的人物（持机者、侧后配角），**不展开** 定妆外貌/衣着/姿态 |
| **只写变化** | 续拍轨正文写 **本轨新增动作/口播**（如强哥走来、对镜说 CTA）；背景人物 **一句「以参考图为准」** 即可 |
| **举一反三** | 任何 `@图片 = 作为首帧`、I2V 接力、视频延长衔接 — **均以上传图为准**，不用文字重描可能与成片不符的细节 |

### 3 · POV 与 Spatial 自洽（谁持机 · 谁在哪一层）

**先画机位**：谁拿镜头 → 机位在谁手中 → 其他人只能在 **景深轴** 上移动，不能「抢镜头」。

| POV 类型 | 持机/机位 | 口播者（非持机） | 禁止写法 |
| --- | --- | --- | --- |
| **业主/阿姨 handheld 自拍** | 阿姨持手机，手臂长度，机位固定 | 强哥 **自画面后方景深向镜头方向走来**，**至中近景正对镜头口播** | ❌ 强哥占自拍前景 / 占主画面 / 抢镜 |
| **强哥对镜口播（阿姨持机）** | 机位仍在阿姨手中，不切 third-person | 强哥 **人走近、画面里变大** = 透视变化，不是换机位 | ❌ 强哥上前占前景（前景属于持机自拍逻辑） |
| **third-person 跟拍** | 摄影机独立 | 主体可占前景、可写 dolly | ❌ 与「手持自拍」混写 |

**空间词对照**（交付块用左列）：

| ✅ 可用 | ❌ 与 handheld 自拍矛盾 |
| --- | --- |
| 深景 / 后方景深 / 向镜头方向走来 / 至中近景对镜 | 占自拍前景 / 占主画面 / 抢镜 / 上前占满画 |
| 阿姨持机 / 机位固定手臂长度 | 镜头推近 / 缓慢推近 / 拉远 / 变焦 |

### 4 · 运镜词与 POV 匹配（手持 ≠ 推轨）

| POV | 允许 | 禁止 |
| --- | --- | --- |
| **手持自拍** | 微晃、随呼吸轻漂、随手势轻漂、不切镜、不变焦、**不推近** | 缓慢推近/拉远、dolly、变焦、三脚架运镜感 |
| **固定/跟拍机位** | dolly in/out、push-in、truck 等 | 与「手持自拍」同段混写 |

> **中文运镜表**里「缓慢推近」**仅用于非手持机位**；手持自拍轨须写 **不推近、机位固定手臂长度**。

### 反模式速查（验收时 grep）

| 若交付块出现… | 判定 | 改法 |
| --- | --- | --- |
| `轨1` / `上一段` / `末帧截图` | ❌ 跨轨 meta | 删；`[H]` 写截图流程 |
| 续拍轨重描阿姨衣着/竖拇指/侧后让位 | ❌ 与截图可能不符 | `以参考图为准` |
| `占自拍前景` / `占主画面` + 阿姨持机 | ❌ 空间矛盾 | `深景→中近景对镜` |
| `缓慢推近` + `手持自拍` | ❌ 运镜矛盾 | `不推近` + `微晃` |
| 续拍轨 `@图片1` 也写 **作为首帧** | ❌ 双首帧抢构图 | **@图片5 作为首帧** + **@图片1 背景环境参考**（分工明确） |
| 续拍轨 `@图片1` 无用途声明 | ❌ 模型不知分工 | 写 **背景环境参考**，不写 **作为首帧** |


## 文档分层 · 交付文本 vs 规范 `[H]`

`-生视频提示词.md` 须分清两层：

| 层 | 典型章节 | 进 Seedance 可复制块？ |
| --- | --- | --- |
| **`[H]` 规范** | 口播铁律、本片规定、上传素材表、验收、翻车修复 | **否** |
| **交付文本** | `## 轨 N · Seedance 可复制块` 内代码块 | **是** |

### 交付文本 · 仅正向描述 · 全中文 · 无小标题

可复制块 **全中文** 交付（运镜、口型、绑定、分 beat 均用中文）。工具 API 参数（`standard`、`generate_audio` 等）只写在 `[H]` 参数表，**不进**可复制块。

可复制块 **只写「要什么」**：参考绑定、场景、运镜、分 beat 表演、口型台词。**不用 `【】` 小标题分段**——绑定、`@` 行置顶（无标题行）；总述、运镜、微动写成连贯直述句；分 beat 以 **`X.XX至X.XX秒，`** 起句直接写表演与台词。

| 写进交付文本 | 留在 `[H]`，不进可复制块 |
| --- | --- |
| 首行 **`禁止字幕，禁止背景音乐。`**（音画输出例外） | 其他约束护栏堆在块末尾 |
| `@图片2 = 强哥五官脸型发型`（绑定区无小标题） | 「本轨不上传 @音频1」 |
| `阿姨口播：四川话女声` / `强哥口播：四川话男声` | 「禁止模仿强哥音色」、**语速 6 字/秒含标点**、字数÷6 换算、`90字15.0秒` 类策划数字 |
| `强哥说：「…」` · performance 后 **`——四川话`** | `Character says`、英文 POV 开场白 |
| `0.00至3.00秒，阿姨说：「…」——…`（时间戳起句，无 beat 标签行） | `0.00-3.00秒·3秒钩子【18字】` 类小标题 |
| `强哥后方嘴闭验厨`（画面状态） | 【摄影·运镜】【微动·A线】【约束护栏】等小标题 |
| `全员自然眨眼每3～5秒`（并入总述或分 beat 直述） | 改稿纠偏：「禁止之前错的东西」 |
| `以上口型与台词一字不改：「…」`（一句收束，非独立标题段） | 验收条目、策划标签 |

**改稿铁律**：修订后的交付文本 **不写** 针对旧稿错误的纠偏句。规范、挂载、验收写在 `[H]`；交付文本用正向替代。

**音画首行**：本仓库 talk-multi / talk-subplot 默认可复制块 **第一行** 写 `禁止字幕，禁止背景音乐。`（仅此二项允许「禁止」句式置顶；其余仍正向直述）。

**微动**：口播真人感用 **`自然眨眼每3～5秒`**，写进总述段或 relevant 分 beat，**不设** `【微动·A线】` 段。

**中文运镜词对照**（可复制块用左列，不用右列英文）：

| 中文（交付文本） | 英文（不进可复制块） |
| --- | --- |
| 不切镜、不变焦、不推近、手持微晃 | no cuts, no zoom, no dolly, handheld |
| 缓慢推近 / 缓慢拉远（**仅非手持机位**） | dolly in / dolly out |
| 一镜连续自拍，前置自拍视角 | One continuous shot, first-person POV |
| 阿姨说 / 强哥说 | Character says |
| 五官与参考图完全一致 | maintain exact appearance from reference image |

> 英文 Seedance POV 模板仅作 skill 参考；本仓库 `-生视频提示词.md` **交付块一律中文**。

### 参考引用 · 仅开头一次

交付文本内 **`@图片` / `@音频` / `{{Mixed N}}` 只在顶部绑定区各写一次**（绑定区 **无** `【参考绑定】` 等小标题行）。绑定之后——总述、运镜、分 beat——**直接描述**人物/场景/音色，不再出现 `@` 或「与@图片N一致」。

| 开头绑定块（仅此一处 `@`） | 绑定之后（直接描述） |
| --- | --- |
| `@图片2 = 强哥五官脸型发型；五官脸型短发工服男，五官与参考图完全一致。` | `强哥——五官脸型短发工服男——后方嘴闭验厨` |
| `@图片1 = 新华苑改完厨房…` | `改完厨房自然窗光，明亮整洁` |
| `@音频1 = 强哥四川话音色参考；强哥口播对口型，成都温江四川话…` | `强哥 says:「…」`（不再提 @音频1） |

身份仍需在分 beat 用**明文**重复（如「五官脸型短发工服男」），只是不再挂 `@` 标签。

## 交付前验收（CHAI 三步 + Seedance 清单）

**Step 1** 完整初稿 → **Step 2** 对照 5 维逐项打分（缺的是故意省略还是遗漏？）→ **Step 3** 修订后才是交付版。

Seedance 专项（摘自 `seedance-2-0` Verification checklist）：

- [ ] 动作在目标时长内连贯可读
- [ ] 音频/对白与画面同步（若 `generate_audio=true`）
- [ ] 角色身份与参考图 / 前镜一致
- [ ] 运镜与 prompt 一致（未擅自 dolly / 变焦）
- [ ] 未依赖模型渲染可读文字 / logo
- [ ] 光色 / 风格与方案一致
- [ ] 时长、画幅（如 9:16）与单轨规划匹配
- [ ] 口播：`[H]` 已算 **≤93 字 · ≤15 秒** · `字数÷6`；**可复制块不写语速/字数换算**
- [ ] 音频参考：**无强哥口播的轨未挂 @音频1**；有强哥口播的轨已挂且未扩散至其他说话人
- [ ] **强哥四川话**：有强哥口播 → 交付块 **强哥口播：四川话男声** · `@音频1` 绑 **四川话** · 强哥分 beat 标 **四川话**
- [ ] 文档分层：`[H]` 规范与 **交付文本** 已分离；可复制块 **无 meta / 无纠偏「禁止…」句**（**首行音画禁止除外**），仅正向描述
- [ ] 结构：可复制块 **无【】小标题** · 分 beat **`X.XX至X.XX秒，`** 直述 · 微动 **自然眨眼每3～5秒** 并入总述/分 beat
- [ ] 音画：首行 **`禁止字幕，禁止背景音乐。`**（若本片要求）
- [ ] 参考引用：`@` **仅开头绑定区各一次**；分 beat / 总述 **无 `@`、无「与@图片N一致」**，身份改直接描述
- [ ] 双轨续拍（若有）：轨 2 **业主手持** · **@图片N 作为首帧** · 以参考图为准 · **无定妆/环境重复 @图片**
- [ ] **单独粘贴测试**：每轨可复制块 **不依赖** 其他轨 / `[H]` · 无「轨N/上一段/末帧来源」
- [ ] **续拍以图为准**：续拍轨 **不展开** 已由截图锁定的人物外貌/姿态
- [ ] **空间自洽**：handheld 自拍下 **非持机者** 不写占自拍前景/占主画面 · 写 **深景→中近景对镜**
- [ ] **运镜自洽**：handheld 自拍 **无推近/拉远/变焦** · 仅微晃 · 机位固定手臂长度

**不通过则回到 Step 3 改 prompt，不得标记为「可生视频」。**

## 禁止

- 未读上述 skill 直接写可复制 prompt 块
- 把 `[H]` 规范、上传策略、纠偏「禁止…」句 **写进** Seedance 可复制块（**首行 `禁止字幕/背景音乐` 除外**）
- 在交付块使用 **`【摄影·运镜】` / `【微动·A线】` / `【约束护栏】` / beat 标签行** 等小标题——改 **时间戳起句直述**
- 在绑定区之后的分 beat / 总述里 **重复写 `@图片` / `@音频` 或「与@图片N一致」**
- **跨轨 meta 进交付块**：「轨1/轨2」「上一段」「末帧截图来源」「与轨N对齐」——仅 `[H]`
- **续拍轨重描截图人物**：定妆衣着/姿态/手势——续拍人物 **以参考图为准**
- **handheld 自拍 + 占自拍前景/占主画面**（非持机者抢镜）——改 **深景→中近景对镜**
- **handheld 自拍 + 缓慢推近/拉远/变焦**——改 **不推近 + 微晃 + 机位固定手臂长度**
- 引用已删除 skill：`seedance-camera`、`director-visual-language`、`lovart-multi`
- 用「发布前自检 / Koda / 薄层句 / 4 格机位板」等旧规范替代 OpenMontage 验收
